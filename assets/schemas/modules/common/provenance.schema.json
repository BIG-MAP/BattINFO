{
  "license": "https://www.apache.org/licenses/LICENSE-2.0",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://w3id.org/battinfo/schema/modules/common/provenance.schema.json",
  "title": "Provenance",
  "description": "Minimal provenance object for shared BattINFO/BDF cell specification files.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "source_file",
    "retrieved_at"
  ],
  "properties": {
    "source_type": {
      "type": "string",
      "description": "Kind of source this record was created from. Recommended values: datasheet, manufacturer, curated, literature, manual, other."
    },
    "source_name": {
      "type": "string",
      "description": "Human-readable name of the source (document title, instrument, lab, or system)."
    },
    "source_file": {
      "type": "string",
      "minLength": 1,
      "description": "Filename or path of the source file, when the record was derived from a file."
    },
    "source_url": {
      "anyOf": [
        {
          "type": "string",
          "format": "uri"
        },
        {
          "type": "null"
        }
      ],
      "description": "URL of the source document or landing page, when available."
    },
    "citation": {
      "type": "string",
      "format": "uri",
      "description": "Resolvable citation target to use for attribution, typically a DOI URL."
    },
    "citation_doi": {
      "type": "string",
      "pattern": "^10\\.\\d{4,9}/[-._;()/:A-Za-z0-9]+$",
      "description": "Deprecated compatibility field. Prefer provenance.citation with a DOI resolver URL."
    },
    "retrieved_at": {
      "type": "integer",
      "minimum": 0,
      "description": "Unix timestamp (seconds since epoch, UTC) when the source was retrieved or the record was captured."
    },
    "workflow_version": {
      "type": "string",
      "description": "Version of the ingest/curation workflow that produced this record."
    },
    "comment": {
      "type": "string",
      "description": "Free-text comment."
    },
    "battinfo_version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+(-[A-Za-z0-9.-]+)?$",
      "description": "Version of the battinfo library that wrote this record (semantic versioning); stamped on save."
    }
  }
}
