{
  "license": "https://www.apache.org/licenses/LICENSE-2.0",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://w3id.org/battinfo/schema/material.schema.json",
  "title": "BattINFO Material Instance",
  "description": "A physical lot/batch of a material that realizes a material spec. The 'instance' half of the spec+instance pair.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "material",
    "provenance"
  ],
  "properties": {
    "schema_version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+(-[A-Za-z0-9.-]+)?$",
      "description": "Version of the record schema this document conforms to (semantic versioning); stamped by the library on save."
    },
    "material": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "material_spec_id"
      ],
      "properties": {
        "id": {
          "$ref": "#/$defs/MaterialIri"
        },
        "material_spec_id": {
          "$ref": "#/$defs/MaterialSpecIri",
          "description": "IRI of the material-spec this physical lot realizes."
        },
        "short_id": {
          "$ref": "#/$defs/ShortId"
        },
        "name": {
          "type": "string",
          "description": "Human-readable label for this physical lot (e.g. a lab/inventory id)."
        },
        "lot_id": {
          "type": "string",
          "description": "Manufacturer / supplier lot or batch number."
        },
        "batch_id": {
          "type": "string",
          "description": "Internal lab batch identifier, when different from the supplier lot."
        },
        "supplier": {
          "$ref": "#/$defs/OrgRef",
          "description": "Supplier or vendor the item was sourced from."
        },
        "received_date": {
          "$ref": "#/$defs/FlexDate",
          "description": "Date the lot was received (YYYY, YYYY-MM, or YYYY-MM-DD, or Unix timestamp)."
        },
        "opened_date": {
          "$ref": "#/$defs/FlexDate",
          "description": "Date this lot was first opened / put into use."
        },
        "expires_at": {
          "$ref": "#/$defs/FlexDate",
          "description": "Expiry or retest date for the lot (YYYY, YYYY-MM, or YYYY-MM-DD, or Unix timestamp)."
        },
        "amount": {
          "$ref": "modules/common/quantity.schema.json",
          "description": "Quantity of this lot on hand or consumed (e.g. mass in g, volume in mL)."
        },
        "storage": {
          "type": "string",
          "description": "Storage conditions for this lot (e.g. 'argon glovebox', 'dry room, -40 degC dew point')."
        },
        "processing": {
          "$ref": "#/$defs/Processing"
        },
        "datasets": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/DatasetLink"
          },
          "uniqueItems": true,
          "description": "Characterization datasets for this material lot (XRD, SEM, ICP, PSD, ...)."
        },
        "property": {
          "$ref": "modules/common/quantitative-properties.schema.json",
          "description": "Named quantity map of technical properties (snake_case key to value+unit quantity)."
        },
        "comment": {
          "type": "string",
          "description": "Free-text comment."
        }
      },
      "description": "The physical material lot being described."
    },
    "provenance": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "source_type"
      ],
      "properties": {
        "source_type": {
          "type": "string",
          "enum": [
            "datasheet",
            "manufacturer",
            "measurement",
            "lab",
            "literature",
            "manual",
            "other"
          ],
          "description": "Kind of source this record was created from."
        },
        "source_name": {
          "type": "string",
          "description": "Human-readable name of the source (document title, instrument, lab, or system)."
        },
        "source_file": {
          "type": "string",
          "description": "Filename or path of the source file, when the record was derived from a file."
        },
        "source_url": {
          "oneOf": [
            {
              "type": "string",
              "format": "uri"
            },
            {
              "type": "null"
            }
          ],
          "description": "URL of the source document or landing page, when available."
        },
        "citation": {
          "type": "string",
          "format": "uri",
          "description": "Resolvable citation target to use for attribution, typically a DOI URL."
        },
        "citation_doi": {
          "type": "string",
          "pattern": "^10\\.\\d{4,9}/[-._;()/:A-Za-z0-9]+$",
          "description": "DOI of the source publication (bare '10.xxxx/...' form). Prefer provenance.citation with a DOI resolver URL."
        },
        "retrieved_at": {
          "$ref": "#/$defs/UnixTime",
          "description": "Unix timestamp (seconds since epoch, UTC) when the source was retrieved or the record was captured."
        },
        "file_hash": {
          "type": "string",
          "pattern": "^[A-Fa-f0-9]{64}$",
          "description": "SHA-256 hash of the source file, for integrity verification."
        },
        "workflow_version": {
          "type": "string",
          "description": "Version of the ingest/curation workflow that produced this record."
        },
        "curated_by": {
          "type": "string",
          "description": "Name or identifier of the person who curated this record."
        },
        "comment": {
          "type": "string",
          "description": "Free-text provenance comment."
        },
        "battinfo_version": {
          "type": "string",
          "pattern": "^\\d+\\.\\d+\\.\\d+(-[A-Za-z0-9.-]+)?$",
          "description": "Version of the battinfo library that wrote this record (semantic versioning); stamped on save."
        }
      },
      "description": "Where the information in this record came from: source type, file or URL, citation, and retrieval time."
    },
    "notes": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Free-text notes carried with the record."
    },
    "funding": {
      "$ref": "#/$defs/Funding"
    },
    "contributor": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Person"
      },
      "minItems": 1,
      "description": "People who contributed this record to the platform (attribution/provenance). Each is a schema:Person; an ORCID may be given in same_as. Stamped by ws.save."
    },
    "license": {
      "type": "string",
      "minLength": 1,
      "description": "License under which this record is released, as an SPDX identifier (e.g. \"cc-by-4.0\") or a URL. Stamped from the workspace default (ws.license) and emitted as dcterms:license in JSON-LD."
    }
  },
  "$defs": {
    "Processing": {
      "type": "object",
      "additionalProperties": false,
      "description": "How this lot was processed into an electrode/coating. Processing is a property of the instance/build, NOT of the spec identity (aqueous vs NMP is not a distinct product).",
      "properties": {
        "route": {
          "type": "string",
          "enum": [
            "aqueous",
            "nmp",
            "dry",
            "other"
          ],
          "description": "Processing route: water-based (aqueous), NMP solvent-based, solvent-free (dry), or other."
        },
        "solvent": {
          "type": "string",
          "description": "Processing solvent (e.g. 'water', 'NMP')."
        },
        "detail": {
          "type": "string",
          "description": "Free-text processing detail (mixing, drying, calendering, ...)."
        }
      }
    },
    "UnixTime": {
      "type": "integer",
      "minimum": 0,
      "description": "Unix timestamp in whole seconds since 1970-01-01T00:00:00Z."
    },
    "FlexDate": {
      "oneOf": [
        {
          "$ref": "#/$defs/UnixTime"
        },
        {
          "type": "string",
          "description": "ISO 8601 calendar date string: YYYY, YYYY-MM, or YYYY-MM-DD.",
          "pattern": "^\\d{4}(-\\d{2}(-\\d{2})?)?$"
        }
      ],
      "description": "Date as an ISO 8601 string (YYYY, YYYY-MM, or YYYY-MM-DD) or a Unix timestamp in seconds."
    },
    "ShortId": {
      "type": "string",
      "pattern": "^[0-9a-hjkmnp-tv-z]{6,16}$",
      "description": "Short human-friendly identifier derived from the record IRI (Crockford base32, 6-16 characters)."
    },
    "MaterialIri": {
      "type": "string",
      "pattern": "^https://w3id\\.org/battinfo/material/[0-9a-hjkmnp-tv-z]{4}(?:-[0-9a-hjkmnp-tv-z]{4}){3}$",
      "description": "Canonical BattINFO IRI in the material/ namespace (https://w3id.org/battinfo/material/xxxx-xxxx-xxxx-xxxx)."
    },
    "MaterialSpecIri": {
      "type": "string",
      "pattern": "^https://w3id\\.org/battinfo/(?:spec|material-spec|electrode-spec|separator-spec|electrolyte-spec|current-collector-spec|housing-spec)/[0-9a-hjkmnp-tv-z]{4}(?:-[0-9a-hjkmnp-tv-z]{4}){3}$"
    },
    "DatasetIri": {
      "type": "string",
      "pattern": "^https://w3id\\.org/battinfo/dataset/[0-9a-hjkmnp-tv-z]{4}(?:-[0-9a-hjkmnp-tv-z]{4}){3}$",
      "description": "Canonical BattINFO IRI in the dataset/ namespace (https://w3id.org/battinfo/dataset/xxxx-xxxx-xxxx-xxxx)."
    },
    "DatasetLink": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id"
      ],
      "properties": {
        "id": {
          "$ref": "#/$defs/DatasetIri",
          "description": "Canonical IRI of the linked dataset record."
        },
        "role": {
          "type": "string",
          "enum": [
            "raw",
            "processed",
            "report",
            "metadata",
            "other"
          ],
          "description": "Role of the linked dataset as a whole relative to this record (record level; the per-file role lives on the dataset's distribution entries)."
        }
      },
      "description": "Link to a dataset record, with the role the dataset plays for this record."
    },
    "OrganizationIri": {
      "type": "string",
      "pattern": "^https://w3id\\.org/battinfo/organization/[0-9a-hjkmnp-tv-z]{4}(?:-[0-9a-hjkmnp-tv-z]{4}){3}$",
      "description": "Canonical BattINFO IRI in the organization/ namespace (https://w3id.org/battinfo/organization/xxxx-xxxx-xxxx-xxxx)."
    },
    "Organization": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name"
      ],
      "properties": {
        "type": {
          "const": "Organization",
          "description": "JSON-LD type discriminator."
        },
        "id": {
          "$ref": "#/$defs/OrganizationIri",
          "description": "Canonical BattINFO organization IRI linking this party to its organization record."
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Organization name."
        },
        "url": {
          "type": "string",
          "format": "uri",
          "description": "Homepage URL."
        }
      },
      "description": "An organization referenced by name, with an optional canonical BattINFO organization IRI."
    },
    "OrgRef": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "$ref": "#/$defs/Organization"
        }
      ],
      "description": "An organization, as either a plain name string or a structured Organization object that can carry a canonical organization IRI."
    },
    "Funding": {
      "type": "object",
      "additionalProperties": false,
      "description": "Funding project (grant) under which the record was produced. Maps to schema:funding / schema:Grant in JSON-LD.",
      "required": [
        "identifier"
      ],
      "properties": {
        "type": {
          "const": "Grant"
        },
        "id": {
          "type": "string",
          "format": "uri"
        },
        "identifier": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string"
        },
        "acronym": {
          "type": "string"
        },
        "program": {
          "type": "string"
        },
        "funder": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "name"
          ],
          "properties": {
            "type": {
              "const": "Organization"
            },
            "name": {
              "type": "string",
              "minLength": 1
            },
            "url": {
              "type": "string",
              "format": "uri"
            },
            "same_as": {
              "type": "string",
              "format": "uri"
            }
          }
        }
      }
    },
    "Person": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name"
      ],
      "properties": {
        "type": {
          "const": "Person"
        },
        "name": {
          "type": "string",
          "minLength": 1
        },
        "given_name": {
          "type": "string",
          "minLength": 1
        },
        "family_name": {
          "type": "string",
          "minLength": 1
        },
        "email": {
          "type": "string",
          "format": "email"
        },
        "url": {
          "type": "string",
          "format": "uri"
        },
        "same_as": {
          "type": "string",
          "format": "uri"
        },
        "affiliation": {
          "$ref": "#/$defs/Organization"
        }
      }
    }
  }
}
