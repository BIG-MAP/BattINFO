Reference
=========

Facts about the surface: every class, command, schema, property, and
guarantee. Orient yourself with the Python API overview, then drill into the
generated references — they are produced from the code and mapping tables and
drift-checked in CI, so they cannot rot.

.. toctree::
   :maxdepth: 1

   ../python-api
   api-reference
   property-reference
   cli-reference
   schema-reference
   ../validation-contract
