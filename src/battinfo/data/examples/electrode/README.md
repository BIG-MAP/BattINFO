# Electrode Instances (Examples)

Physical electrodes (built sheets/discs) — the *instance* half of the `electrode-spec` +
`electrode` pair. Each links to its `electrode_spec` via `electrode_spec_id` and carries
build facts (lot id, diameter, mass) and optional characterization `datasets`.
