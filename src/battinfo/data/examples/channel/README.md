# Channel (Examples)

One channel of one equipment unit. Instance-only (no channel-spec kind): flat IRIs under `channel/`, parent link `equipment_id` in the record body, uid minted deterministically from (equipment uid, index) so registration is idempotent.
