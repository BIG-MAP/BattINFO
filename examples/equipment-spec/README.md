# Equipment Spec (Examples)

Reusable lab-equipment model descriptions (e.g. the Neware BTS-4000 cycler model). The spec half of equipment-spec + equipment; mints under the shared `spec/` namespace. Equipment category (cycler, glovebox, ...) is data in `equipment_class`, never a namespace segment.
