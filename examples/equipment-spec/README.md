# Equipment Spec (Examples)

Reusable lab-equipment model descriptions (e.g. the SkyRC MC3000 charger-analyzer model). The spec half of equipment-spec + equipment; mints under the shared `spec/` namespace. Equipment category (cycler, glovebox, ...) is data in `equipment_class`, never a namespace segment.
