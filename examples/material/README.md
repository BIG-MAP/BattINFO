# Material Instances (Examples)

Physical material lots/batches — the *instance* half of the `material-spec` + `material`
spec/instance pair. Each record links to its `material_spec` via `material_spec_id` and
carries lot-specific facts (supplier lot id, measured mass, received date).
